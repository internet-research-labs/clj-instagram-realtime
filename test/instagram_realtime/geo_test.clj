(ns instagram-realtime.geo-test
  (:require [clojure.test :refer :all]
            [instagram-realtime.geo :as geo]))


(deftest test-geo-subscribe
  (testing "geo/subscribe"
    (testing "should execture without breaking")))
